# 揭秘数据驱动的概率性中期天气预报

**Demystifying Data-Driven Probabilistic Medium-Range Weather Forecasting**

> **作者**：Jean Kossaifi\*, Nikola Kovachki\*, Morteza Mardani, Daniel Leibovici, Suman Ravuri, Ira Shokar, Edoardo Calvello, Mohammad Shoaib Abbas, Peter Harrington, Ashay Subramaniam, Noah Brenowitz, Boris Bonev, Wonmin Byeon, Karsten Kreis, Dale Durran, Arash Vahdat, Mike Pritchard, Jan Kautz
>
> **机构**：NVIDIA（\* 共同一作）
>
> **日期**：2026 年 1 月 26 日 | **arXiv**：[2601.18111](https://arxiv.org/abs/2601.18111)

---

## 摘要

近年来，数据驱动天气预报方法的革新浪潮催生了大量结构各异的复杂架构与训练策略，遮蔽了影响预报精度的根本驱动因素。本文证明：要实现最先进的概率预报技能，既不需要复杂的架构约束，也不需要专门的训练启发式方法。我们提出了一个可扩展框架，通过将直接下采样的潜空间与历史条件局部投影器相结合，学习多尺度大气动力学，从而解析高分辨率物理过程。我们发现该框架设计对概率估计器的选择具有鲁棒性，可无缝支持随机插值、扩散模型和基于 CRPS 的集合训练。与集成预报系统（IFS）及深度学习概率模型 GenCast 相比，我们的框架在大多数变量上取得了统计显著性的改进。结果表明，扩展通用模型即可达到中期预报的最先进水平，无需量身定制的训练方案，且在概率框架的完整谱系上均表现有效。

---

## 1. 引言

直到不久以前，大气科学界的共识还是：纯数据驱动方法永远无法媲美数值天气预报（NWP）的精度。大气混沌的多尺度动力学被认为必须依赖基于流体力学的显式求解器。然而，神经算子方法的引入，以及随后图神经网络方法的涌现，证明深度学习不仅能从历史数据中捕捉全球环流模式，还能以媲美欧洲中期天气预报中心（ECMWF）开发的"金标准"集成预报系统（IFS）的精度做到这一点。此后，数据驱动确定性模型已达到或超越传统 NWP 的水平。

除了与传统求解器精度相当外，这些模型还拥有根本性的计算优势：生成预报的速度快数个数量级，通常只需单张 GPU 即可运行。这一效率使高质量预报得以普及，并为大规模集合预报开辟了道路。因此，研究前沿已从"证明可行性"转向"掌握概率预报"，旨在捕捉部分可观测混沌系统中固有的不确定性。

然而，该领域近期的快速演进导致了架构与训练方法论的碎片化格局。由于天气数据定义在球面流形上，并涉及不同的物理变量，研究者高度重视领域特定的归纳偏置。当前方法通常归入三种不同的设计哲学：

---

### 背景补充：三种主流数据驱动天气预报架构

#### 第一类：谱/卷积模型（FourCastNet 系列）

**核心思路**：大气数据定义在球面上，地球不是平面，因此普通的卷积（假设平面周期性）会在极点附近产生失真。这一类方法的思路是：与其事后修正，不如从一开始就让模型"知道"自己在处理球面数据。

**技术手段**：球谐函数（Spherical Harmonics）是球面上的"傅里叶基"——就像普通傅里叶变换把信号分解为正弦波，球谐变换把球面上的场分解为不同频率的球面波。在球谐域做卷积，数学上天然满足球面旋转等变性（即把地球转一个角度，模型输出也跟着转同样角度），这种性质称为**等变性（equivariance）**，是一种强归纳偏置。

**代表模型**：
- **FourCastNet**（NVIDIA，2022）：首个达到 IFS 水平的数据驱动模型，使用自适应傅里叶神经算子（AFNO）；
- **FourCastNet 2 / SFNO**：改用球面傅里叶神经算子（Spherical FNO），更严格地施加球面等变性；
- **FourCastNet 3**（2025）：进一步扩展到概率预报，使用 CRPS + 谱正则化训练集合。

> **插曲：谱/卷积模型本身是确定性的，怎么做集合预报？**
>
> FourCastNet 1/2 确实是确定性模型——单次前向传播只输出一个预报场，没有随机性。要产生集合，有两种思路：
>
> - **初始条件扰动**：对输入加不同的随机扰动，重复跑多次得到多个成员。这是传统数值天气预报集合的经典做法，数据驱动模型可以直接套用，代价是推理时间随集合规模线性增长。
> - **模型内部引入随机性**：在网络里加入一个可采样的噪声向量作为额外输入，每次采样不同的噪声就得到不同的预报成员，单次前向传播即为一个集合成员。
>
> FourCastNet 3 采用的是第二种方式——在球谐/卷积骨干的基础上，额外引入噪声输入，再用 CRPS + 谱正则化来训练这个随机生成过程，使集合统计上校准、频谱上真实。骨干架构本身（球谐卷积）负责捕捉大气动力学，随机噪声负责表达不确定性，两者分工明确。

> **什么是 CRPS + 谱正则化集合训练？**
>
> **集合预报（Ensemble Forecasting）** 是概率天气预报的核心思路：不输出单一的确定性预报，而是同时输出多个成员（ensemble members），用这些成员的分布来表达不确定性。比如输出 50 个预报成员，某地明天降水的概率就可以用"50 个成员中有多少个预报降水"来估计。
>
> **CRPS（Continuous Ranked Probability Score，连续概率评分）** 是评价和训练集合预报最常用的指标。直观理解：
> - 若集合成员的预测值普遍接近真实观测 → CRPS 小（好）；
> - 若集合成员分散、覆盖了真实观测所在范围 → CRPS 也会奖励合理的不确定性；
> - 若集合过于分散（随便乱猜）→ CRPS 会惩罚。
>
> 数学上，对于预测集合和真实值 $y$，CRPS 可以写成：
> $$\mathcal{L}_{\text{CRPS}} = \mathbb{E}\big[|f(\xi) - y|\big] - \frac{1}{2}\mathbb{E}\big[|f(\xi) - f(\xi')|\big]$$
> 第一项让集合成员尽量靠近真实值，第二项惩罚成员之间过于相似（鼓励保留适当离散度）。用 CRPS 作为损失函数直接训练神经网络，就叫 **CRPS 训练**。
>
> **问题：谱偏差（Spectral Bias）**。单独用 CRPS 训练时，模型倾向于预报"大尺度平滑"的场——全局趋势（气旋在哪、高压在哪）预报得不错，但小尺度细节（局部强降水、锋面锐利边界）会被"抹平"，因为平滑的预报在 CRPS 意义上已经足够好了。这体现在频谱上：高频分量的能量被系统性地低估。
>
> **谱正则化（Spectral Regularization）** 的思路：在 CRPS 损失之外，额外加一个在**频谱域**计算的惩罚项。具体做法是对预测场做球谐变换（分解成不同空间频率的分量），要求预测的各频率分量的能量分布与真实观测的频谱一致。这样，即使模型在物理空间上倾向于输出平滑场，频谱约束也会强迫它补回高频细节。
>
> **两者结合**：CRPS 保证集合的统计校准性（预测的不确定性范围与实际误差匹配），谱正则化保证预测场的物理细节丰富度（不过度平滑）。FourCastNet 3 和本文的 ATLAS-CRPS 都采用了这一组合。

**优势**：谱方法天然捕捉全局模式（大气的行星波、急流等），计算高效；  
**缺点**：球谐变换实现复杂，高分辨率下计算量大；谱截断会丢失小尺度信息。

---

#### 第二类：图神经网络模型（GraphCast / GenCast 系列）

**核心思路**：与其强迫数据适应规则网格，不如用一种更"自然"的方式表示球面——二十面体网格。二十面体（icosahedron）是一个 20 面多面体，对其面进行细分可以得到近似均匀覆盖球面的三角形网格，不像经纬度网格那样在极点附近过度密集。

**二十面体网格的构造**：从一个正二十面体（20 个等边三角形面）出发，对每个三角形面反复细分（每次细分将一个三角形拆成 4 个小三角形），然后把所有顶点投影到球面上。GraphCast 使用 6 级细分，产生约 40,962 个节点，节点间距约 100 km，分布比经纬度网格均匀得多——经纬度网格在赤道附近节点稀疏、在极点附近节点极度密集，而二十面体网格全球密度基本一致。

**技术手段**：

**第一步——Grid→Mesh（编码器）：ERA5 数据如何投影到二十面体网格**

ERA5 数据在规则经纬度网格上（如 $721\times1440$），而二十面体网格的节点位置与经纬度网格的点**几乎从不重合**，所以不能直接赋值，需要一个显式的映射过程。GraphCast 的做法是：

- 对每个**网格节点**（grid node，即经纬度网格上的点），在球面上找到其附近一定半径内的所有**网格格点**（grid cells，即 ERA5 原始格点），连一条从 ERA5 格点指向网格节点的有向边；
- 每条边携带两端点的**相对位置特征**（球面上的方向向量和距离）；
- 用一个**学习到的 MLP** 处理这些边特征，并做加权聚合——相当于让网格节点"收集"周围 ERA5 格点的信息，学习如何加权组合它们。

这个过程不是双线性插值，而是**通过训练学到的空间聚合**，模型可以根据任务自适应地决定如何整合周边信息。

**第二步——Mesh→Mesh（处理器）：多尺度消息传递**

在二十面体网格内部做多轮消息传递，每个节点反复与其邻居交换信息。GraphCast 同时维护**多分辨率的网格层级**（从粗到细），粗层级上的消息传递让信息快速跨越长距离（捕捉大尺度环流），细层级上的消息传递处理局部细节。

**第三步——Mesh→Grid（解码器）：映射回经纬度输出**

方向反过来：对每个**ERA5 输出格点**，找附近的网格节点，用另一个 MLP 做加权聚合，得到该格点的预报值。结构上与编码器对称，但参数独立。

**代表模型**：
- **GraphCast**（Google DeepMind，2023）：确定性预报，首次在多数指标上超越 IFS；
- **GenCast**（Google DeepMind，2024）：概率版本，在图模型基础上加入扩散模型，目前是最强的公开概率基线之一；
- **AIFS**（ECMWF，2024）：ECMWF 官方的数据驱动模型，同样基于图结构；
- **Alet et al. 2025**（Aardvark Weather）：在图模型上进一步改进概率预报技巧。

---

> **GenCast 具体细节**
>
> GenCast 的核心思路是：GraphCast 已经能很好地建模大气确定性动力学，在它的基础上叠加一个扩散模型来建模不确定性，两者分工明确。
>
> **数据设置**
> - 训练数据：ERA5，1979–2018 年训练，2019–2020 年验证/测试；
> - 分辨率：0.25 度（$721\times1440$），**12 小时步长**（ATLAS 是 6 小时）；
> - 输入：当前时刻 $t$ 和前一时刻 $t-12\text{h}$ 共两帧，输出：下一时刻 $t+12\text{h}$。
>
> **架构：Graph + Diffusion 的叠加**
>
> GenCast 保留了 GraphCast 的编码器-处理器-解码器骨架，但在其中叠加了扩散模型。两者的关系是：**消息传递（图神经网络）是计算机制，扩散（去噪）是训练目标**——去噪网络本身就是一个图神经网络，它用消息传递来处理"带噪的 $\Delta x$ + 当前状态条件"，输出去噪后的 $\Delta x$。具体流程如下：
>
> **训练阶段**（让模型学会"大气在一步内如何变化"）：
>
> 1. 从训练集取一对相邻帧 $(x_t,\ x_{t+12\text{h}})$，计算真实残差 $\Delta x = x_{t+12\text{h}} - x_t$；
> 2. 对 $\Delta x$ 逐步加高斯噪声，得到带噪样本；
> 3. 用 Grid→Mesh 编码器将 $x_t$ 和 $x_{t-12\text{h}}$ 编码为条件 $c$；
> 4. 训练去噪网络：以带噪的 $\Delta x$、噪声水平 $\sigma$、条件 $c$ 为输入，预测干净的 $\Delta x$（或等价地预测噪声）。
>
> **推理阶段**（自回归生成一条完整预报轨迹）：
>
> 对于每一个自回归步，流程如下：
>
> 1. 将当前已知的 $x_t$（和上一帧 $x_{t-12\text{h}}$）编码为条件 $c$；
> 2. 从纯高斯噪声 $\epsilon \sim \mathcal{N}(0, I)$ 出发，在条件 $c$ 的引导下反复去噪 **20 步**，生成一个 $\Delta x$ 的样本；
> 3. **将 $\Delta x$ 加回当前状态**，得到下一帧预报：$\hat{x}_{t+12\text{h}} = x_t + \Delta x$；
> 4. 以 $\hat{x}_{t+12\text{h}}$ 作为新的"当前状态"，重复步骤 1–3，滚动到 15 天。
>
> $\Delta x$ 本身不是最终目标，它只是"桥梁"——每一步通过扩散采样一个大气变化量，加到当前状态上往前推进一帧。
>
> **集合是怎么产生的**：上面步骤 2 中的初始噪声 $\epsilon$ 每次独立采样，去噪路径不同，生成的 $\Delta x$ 也不同，因此每次自回归展开都走向一条不同的轨迹。跑 56 次独立展开，就得到 56 条不同的预报轨迹，即 56 个集合成员。
>
> **为什么对 $\Delta x$ 而不是 $x_{t+12\text{h}}$ 建模**
>
> 大气状态本身的绝对值很大（位势高度动辄数千米，气压数百 hPa），而 12 小时内的变化量 $\Delta x$ 是小量，分布方差远小于状态本身。扩散模型在小方差的分布上学习更稳定、生成质量更高。ATLAS 选择对残差建模的原因完全相同。
>
> **推理代价分析**
>
> 生成一个集合成员需要 20 步去噪，每步一次完整的图神经网络前向传播。15 天预报共 30 个自回归步（12h × 30 = 15 天），若生成 56 个成员：
>
> $$56 \text{ 成员} \times 30 \text{ 步} \times 20 \text{ 去噪步} = 33{,}600 \text{ 次前向传播}$$
>
> 这是 GenCast 单步耗时约 140 秒的根本原因。对比 ATLAS-CRPS 只需 1 次前向传播（3.3 秒/步），代价相差约 40 倍。
>
> **与 ATLAS 的关键区别**
>
> | | GenCast | ATLAS-EDM |
> |---|---|---|
> | 骨干架构 | 图神经网络（二十面体网格）| Transformer（下采样规则网格）|
> | 扩散空间 | 接近原始分辨率的残差 | 16× 下采样的潜空间残差 |
> | 时间步长 | 12 小时 | 6 小时 |
> | 去噪步数 | ~20 步 | ~20 步 |
> | 单步推理 | ~140 秒 | ~88 秒 |
>
> ATLAS 在潜空间做扩散的优势在于：序列长度从 $721\times1440$ 降到 $181\times360$，序列长度缩小 16 倍，使得 Transformer 的全局注意力变得可行，同时扩散过程在低维空间中更稳定。

**优势**：网格均匀，极点无畸变；消息传递自然建模局部物理过程；多尺度架构捕捉不同尺度大气动力学；  
**缺点**：图结构使并行化比规则网格更难；编码器-处理器-解码器的三段式架构工程复杂；推理速度较慢（GenCast 单步 ~140 秒）。

---

#### 第三类：视觉 Transformer 改造（Pangu-Weather / FengWu 系列）

**核心思路**：ViT（Vision Transformer）在图像领域大获成功——把图像切成小块（patch），用 Transformer 的全局自注意力建模块间关系。天气场本质上也是二维（或三维）空间场，为什么不直接复用这套成熟框架？

**挑战**：天气数据比图像复杂得多：
- **三维结构**：大气有多个气压层（垂直维度），不同高度的物理过程相互耦合；
- **多变量异质性**：温度、风速、湿度等物理量量纲和分布差异巨大；
- **球面几何**：纬度越高，网格单元的实际面积越小，简单的矩形分块会忽视这一点。

**各模型的不同解法**：
- **Pangu-Weather**（华为，2023）：引入三维地球位置嵌入（3D Earth Position Embedding），将气压层作为第三个空间维度；使用三维窗口注意力（3D window attention），让不同气压层的信息在注意力窗口内直接交互；同时用四个独立模型分别预报 1h/3h/6h/24h，用层次化方式减少自回归步数；
- **FengWu**（上海 AI Lab，2023）：在 ViT 基础上加入跨模态注意力（cross-modal attention），分别处理地面变量和高空变量，再交互融合；
- **ClimaX**（微软，2023）：针对气候任务的通用预训练模型，设计了变量分词（variable tokenization）——每个变量在每个位置单独成为一个 token，加入变量类型嵌入；优势是灵活支持不同输入变量组合。

**优势**：全局自注意力理论上能捕捉任意远程相关性；工程实现成熟，可直接借用 LLM/视觉大模型的优化经验；  
**缺点**：直接在高分辨率天气数据（如 $721\times1440$ 网格）上做全局注意力，序列长度达百万级，计算代价极高；各模型因此都引入了复杂的分块策略或局部窗口来降低复杂度，但这又引入了新的工程约束。

---

**三类方法的共同问题**（也是 ATLAS 要解决的）：都引入了大量领域特定的工程约束，导致：一是难以扩展（scaling），无法像 LLM 那样简单地堆参数就能提升性能；二是各方法与特定损失函数或训练方案紧耦合，难以迁移到新的概率估计框架。

---

- **第一类**：FourCastNet 系列及相关架构，利用球谐函数和卷积严格施加几何对称性作为归纳偏置；
- **第二类**：基于图的模型，将大气编码在二十面体网格上，依靠消息传递架构捕捉物理动力学；
- **第三类**：对视觉 Transformer（ViT）进行改造，采用特制的分词策略或垂直聚合层来处理大气三维结构。

尽管这些专门化的架构和训练约束是有效的，它们往往引入了显著的工程复杂性和计算瓶颈，阻碍了高效扩展。

在概率预报场景下，挑战因设计空间的进一步碎片化而加剧。为捕捉不确定性，研究者提出了复杂的损失函数和采样策略。一些方法直接优化连续概率评分（CRPS），但在最短保留波长处预测谱功率时可能存在困难；另一些方法加入谱项以改善清晰度。更近期的扩散模型、流匹配或一致性模型展示了高保真度，但推理速度较慢或分辨率受限。实际上，扩散方法在可控性方面具有特别吸引人的潜力，这与超出预报任务的多任务基础建模高度相关。然而，迄今为止，若不牺牲时间或空间分辨率，扩散方法尚未达到接近最先进水平的概率预报技能——这是一个悬而未决的挑战。

我们通过追求一条截然不同的路径来探究：是否任何特定的概率估计方法、训练方案或架构复杂性，对于达到最先进天气预报都是必要的——这条路径就是一个依赖标准可扩展组件的精简框架。我们的框架利用在压缩潜空间中运行的通用 Transformer 架构。受潜扩散在计算机视觉中成功经验的启发，我们的方法将全局动力学建模与高分辨率合成解耦。我们的方法摒弃领域特定约束，转而采用通用可扩展骨干网络，以简洁性实现了最先进的概率性能。

**主要贡献如下**：

- **统一潜空间框架**：我们发现将扩散或随机插值等概率方法直接应用于高分辨率天气数据，在计算上往往不可行或不稳定。我们提出 ATLAS 框架（Atmospheric Transformer in Latent Space）：在直接下采样潜空间中运行的标准 Transformer，加上一个历史条件局部投影器。我们证明该精简框架能有效解析高分辨率物理过程，无需复杂的变分编码或特制层。

- **方法无关的鲁棒性**：我们证明该框架可作为概率预报的通用骨干网络。与之前紧密耦合特定损失函数或训练方案的工作不同，我们的框架使用三种独立的估计方法（随机插值、扩散模型、基于 CRPS 的集合训练）均取得了最先进结果，这表明有望统一碎片化的建模格局。

- **完全开放模型的最先进性能**：我们的方法大幅超越 IFS，并对大多数变量相对 GenCast 取得了统计显著性改进。

- 这一性能代表了迄今为止在不牺牲分辨率的情况下训练基于扩散的方法完成此任务的最成功尝试。

---

## 2. ATLAS：中期潜空间概率天气预报

本节介绍 ATLAS（Atmospheric Transformer in Latent Space），我们的中期概率天气预报方法。我们首先引入问题形式化、潜空间建模的选择和骨干架构，然后介绍用于训练和验证我们方法的三种概率生成方法。

### 2.1 问题形式化

我们考虑一个平稳的离散时间 $\mathbb{R}^d$ 值随机过程 $\{x_j\}_{j \in \mathbb{Z}}$，它表示大气状态 $d$ 个测量量在等间隔时刻的分布。我们的任务是从 $\{x_j\}$ 的单一有限实现中建模条件分布 $\rho_c(x_{j+1}|x_j)$。

注意，由平稳性假设，联合分布 $\rho(x_j, x_{j+1}) = \rho(x_k, x_{k+1})$ 对任意 $j, k \in \mathbb{Z}$ 均成立，因此 $\rho_c$ 与时间无关。这使我们可以将问题形式化为：给定 $N$ 个来自联合分布 $\rho(x_0, x_1)$ 的实现，找到单一条件分布 $\rho_c(x_1|x_0)$ 的模型。具体地，我们假设拥有数据 $\{(x_j^\dagger, x_{j+1}^\dagger)\}_{j=0}^{N-1}$，使得每对 $(x_j^\dagger, x_{j+1}^\dagger) \sim \rho(x_0, x_1)$ 同分布。虽然我们在此框架下形式化模型以求简洁，但我们承认平稳性假设对真实天气数据可能不成立。这一问题可通过对非平稳性来源进行适当条件化来处理，例如，对时刻和年份的某种表示进行条件化。

在 2.4 节中，我们将给出 $\rho_c(x_1|x_0)$ 的三种近似模型。每种方法定义一个条件传输映射，给定来自 $\rho(x_0)$ 的初始条件，将某已知概率分布推送到 $\rho_c(x_1|x_0)$。因此，对 $\rho_c(x_1|x_0)$ 的采样可通过先从已知分布采样、再计算传输映射来完成。在 2.4.1 和 2.4.2 节中，传输映射被隐式定义为以 $\rho_c(x_1|x_0)$ 为终止条件的随机过程的流映射；而在 2.4.3 节中，它被直接定义。每种情形都需要参数化一个映射，该映射以 $x_0$ 及其他依情形而定的输入为输入，产生 $\mathbb{R}^d$ 值输出。在后续各节中，我们将介绍一个统一框架来参数化此类映射，并对所有方法都采用的原始数据降维方式进行介绍。

给定初始样本 $x_0 \sim \rho(x_0)$，我们可以用 2.4 节中的模型生成下一时步的近似样本 $\hat{x}_1 \sim \rho_c(x_1|x_0)$。然后将 $\hat{x}_1$ 视为初始样本，再次用我们的近似模型产生下一时步的样本 $\hat{x}_2 \sim \rho_c(x_1|\hat{x}_1)$。迭代该过程（称为"展开"，unrolling），即得初始条件 $x_0$ 的预报 $\hat{x}_1, \hat{x}_2, \ldots$。由于 $\hat{x}_1$ 仅近似按 $\rho_c(x_1|x_0)$ 分布，将其用作下一步初始样本会引入输入的分布偏移。随着展开步数增加，误差可能累积，分布偏移可能加剧。我们证明我们的架构对这种分布偏移具有鲁棒性，可生成长达 15 天（即 60 个自回归步）的稳定预报。

### 2.2 潜空间建模

#### 潜空间的选择

我们对来自 ECMWF ERA5 数据集的再分析大气观测时间序列建模（详见 3.1 节）。每个数据点由 75 个变量组成，每个变量定义在四分之一度 $721\times1440$ 等角网格上，因此 $d = 77{,}868{,}000$。直接处理如此高维数据对现代深度学习架构在计算上极具挑战性。因此，我们采用潜空间方法——这种方法近期在计算机视觉和自然语言处理任务中取得了成功：将每个数据点编码到低维空间，在该空间进行概率推断，再将输出解码回原始空间。

在计算机视觉中，通常先训练编码器-解码器架构，再在编码后的数据上训练概率模型。**我们采取更简单的方式**：用双线性插值对数据进行下采样，然后训练一个仅解码器模型来上采样。我们发现这种方法能产生行为好得多的潜空间，具有有利的谱性质，使概率预报模型在中期展开时保持稳定，而无需自回归微调。

标准 VAE 会在编码器中混合所有通道（此处代表物理变量），将异质变量聚合到公共通道空间中产生潜特征。然而初步实验表明，这种混合倾向于：**(i)** 产生具有长而平坦高频尾部的潜谱（这是对复杂多通道场进行激进压缩时谱偏差的一种表现）；**(ii)** 忽略数据的内在时间结构，使得时间上相邻的场在潜空间中可能被映射到相距甚远的位置。通过使用双线性插值，我们仅从数据中去除小尺度物理过程（这些过程通常只在短时间尺度可预报），同时保留其他物理结构。

此外，由于原本为编码器保留的内存和计算资源现在可以用于解码器，我们发现该方法实现了显著更好的重建性能。我们选择 $16\times$ 压缩方案：将每个变量双线性插值到一度 $181\times360$ 等角网格。由于我们选择以 6 小时间隔进行预报，这一选择在物理上是合理的——在该时间尺度下，约 70 公里以下的空间尺度是确定性不可预报的（Surcel 2015）。我们发现，在用解码器模型上采样时没有预报性能的损失，因为解码器的重建误差通常比预测模型的误差低一个数量级。我们在 5 节中给出了各种编码策略的消融实验，并与编码器-解码器模型进行了比较。

#### 残差预测与历史信息

实验测试还发现，预测状态的时间残差（而非状态本身）效果更好。我们还发现，加入一个额外的历史状态可以改善模型的鲁棒性，提升长期预报的稳定性。

设 $\mathcal{B}$ 为双线性下采样算子。我们记 $z_j = \mathcal{B}(x_j)$ 为潜态，$r_{j+1} = \mathcal{B}(x_{j+1} - x_j)$ 为潜残差。我们的目标是近似分布 $\rho_c(r_{j+1}|z_j, z_{j-1})$。我们假设 $\mathcal{B}$ 的应用保持平稳性，因此对所有 $j \in \mathbb{Z}$，这等价于分布 $\rho_c(r_1|z_0, z_{-1})$。设 $\mathcal{D}$ 为训练来近似逆 $\mathcal{B}$ 的解码器模型。由此可得，若 $y \sim \rho_c(r_1|z_0, z_{-1})$，则 $\mathcal{D}(y) + x_0$ 近似按 $\rho_c(x_1|x_0, x_{-1})$ 分布。因此，我们训练一个对残差进行上采样的解码器模型，使得 $\mathcal{D}(r_1) \approx x_1 - x_0$。我们发现，将初始状态作为额外输入加入该模型可显著改善重建性能，即 $\mathcal{D}(r_1, x_0) \approx x_1 - x_0$。这使 $\mathcal{D}$ 类似于一个预测残差的确定性模型，但同时能获取下采样（潜空间）版本的真值——此处由我们的概率模型提供。这种数据混合使模型更容易从其输入中捕捉高分辨率细节和必要的物理动力学。

### 2.3 Transformer 架构作为 ATLAS 的骨干

作为基础模型，我们改造了扩散 Transformer（DiT），该架构最初因图像生成而广为人知。我们根据 DiT 的具体细节，构建参数化 $\mathcal{D}$ 的解码器架构，以及用于建模 $\rho_c(r_1|z_0, z_{-1})$ 的预测架构。在潜图像和视频扩散模型中，常见做法是用卷积 U-Net 作为解码器，用 Transformer 作为生成模型。我们发现，使用基于 Transformer 的解码器可显著改善重建结果，误差通常比预测结果低一个数量级。这一改进使我们的潜空间方法成为可能——我们发现从潜空间到原始空间的转换不会造成性能损失。

#### 解码器架构

我们的解码器模型 $\mathcal{D}$ 接受两路输入：
- 潜残差 $r_1$：$181\times360$ 空间场，75 个通道；
- 全分辨率初始条件 $x_0$：$721\times1440$ 空间场，79 个通道（详见 3.1 节）。

输出为残差 $x_1 - x_0$ 的近似，即 $721\times1440$ 空间场，75 个通道。

为对齐两个空间场，我们首先用学习到的步幅卷积（$4\times4$ 核）将 $x_0$ 分块为 $181\times360$ 个 token，将通道扩展到嵌入维度 $e_0 \in \mathbb{N}$。类似地，对残差场 $r_1$ 应用 $1\times1$ 非步幅卷积，将其通道扩展到 $e_1 \in \mathbb{N}$。然后将两个场拼接为单一的 $181\times360$ 空间场，通道数为 $e = e_0 + e_1$。在此场上加入标准正余弦位置编码，然后应用 $l \in \mathbb{N}$ 个 DiT 块。每个 DiT 块被改造为在 $3\times3$ 窗口内使用**局部注意力**，并在注意力操作之前施加最小化的球面一致性填充，以确保真实场中每个像素拥有其 $3\times3$ 的上下文窗口。由于解码器的任务在空间上是局部的，我们发现局部注意力极为有效，同时在序列长度 $181\times360 = 65{,}160$ 上比全局注意力快一个数量级。在应用最后一个 DiT 块后，用一个局部线性层将 $e\times181\times360$ 的场投影回 $75\times721\times1440$ 的场。

由于原始 DiT 是为扩散模型设计的，每个 DiT 块接受一个额外的时间条件参数 $t$，该参数在内部对块输出的尺度和平移进行调制。我们保留这一额外条件，对任意输入简单地固定 $t=1$，因为我们发现这比完全去除该条件表现更好。

#### 预测架构

与解码器架构类似，用于参数化映射 $\hat{b}$、$\hat{s}$ 或 $\hat{f}$ 的预测架构基于 DiT，但具有不同数量的输入场。对于 $\hat{b}$（或 $\hat{s}$），输入为带噪场 $X_t^{\text{SI}}$（或 $X_t^{\text{DM}}$）与两个历史状态 $z_0, z_{-1}$，均为 $75\times181\times360$ 空间场。我们用步幅卷积（$2\times3$ 核）将第一路输入场分块为维度 $e_1\times p_1\times p_2$，用类似卷积将历史状态 $(z_0, z_{-1})$ 的拼接处理为 $e_2\times p_1\times p_2$ 的场。2.4 节提供了进一步的细节。

实验表明，更小的分块尺寸（即更大的 $p_1, p_2$）能持续提升单步预报性能，代价是更高的计算和内存开销。然而，当分块尺寸非常小时，会出现过拟合的迹象，预报在展开时变得不稳定。我们发现 $2\times3$ 分块（使 $p_1=91$，$p_2=120$）是最佳折衷，产生准确且稳定的模型。对于等角网格，这一分块尺寸已知能给出近似各向同性的分块，在气候建模的数值方法中亦有先例（Gettelman 2021）。

分块后，输入被拼接为嵌入维度 $e = e_1 + e_2$、序列长度 $p_1 \cdot p_2 = 10{,}920$ 的单一表示。在此表示上加入标准正余弦嵌入，然后应用 $l \in \mathbb{N}$ 个 DiT 块，每块以时间输入 $t \in \mathbb{R}$ 为条件。在此架构中，我们使用**全局注意力**，因为我们发现它相比局部注意力显著改善了模型的稳定性——这可能是由于大气动力学中潜在的长程相关性。最终状态再次通过一个局部线性层投影到 $75\times181\times360$ 的场。对于 $\hat{f}$ 的情形，输入为场 $z_0, z_{-1}$ 和高斯向量 $\xi \sim \mathcal{N}(0, I_p)$。空间场的处理方式与前述相同，高斯向量则作为额外条件输入到各 DiT 块，类似于 Lang 等人（2024）的架构中的处理方式。

**模型超参数**：

| 组件 | 参数量 | 嵌入维度 $e$ | DiT 块数 $l$ | 注意力头数 | 每头维度 |
|------|--------|------------|------------|----------|--------|
| 预测网络（SI/EDM）| **24 亿** | 3,328（$e_0$=2496，$e_1$=832）| 12 | 13 | 256 |
| 预测网络（CRPS）| **33 亿** | 3,328 | 12 | 13 | 256 |
| 解码器 | **18 亿** | 2,496（$e_0$=1728，$e_1$=768）| 16 | 12 | 208 |

CRPS 模型的噪声维度与嵌入维度相同，$p=3{,}328$，噪声先经 MLP 处理，再作为额外条件传入各 DiT 块，由调制层处理，由此额外引入参数。

### 2.4 潜空间中的三种概率建模方法

以下三个小节描述三种概率方法，每种方法训练完成后都允许我们对潜条件分布 $\rho_c(r_1|z_0, z_{-1})$ 进行近似采样。结合我们的解码器，即可近似对 $\rho_c(x_1|x_0)$ 进行采样，如 2.2 节所述。为符号简洁，我们在形式化中省略 $z_{-1}$，仅指出它作为额外输入加入任意参数化映射。此外，记 $z_j^\dagger = \mathcal{B}(x_j^\dagger)$，$r_{j+1}^\dagger = \mathcal{B}(x_{j+1}^\dagger - x_j^\dagger)$（$0 \leq j \leq N-1$）为数据集的潜编码。为简洁起见，下文仍用 $d$ 表示数据维度，但此处指潜编码的维度，而非原始数据。

#### 2.4.1 随机插值（Stochastic Interpolants, SI）

我们的第一种建模方法基于随机插值（SI）框架，特别是其时间序列形式化。定义随机过程：

$$I_t = \alpha(t) z_0 + \beta(t) r_1 + \sigma(t) W_t, \qquad 0 \leq t \leq 1$$

其中 $(z_0, r_1) \sim \rho(z_0, r_1)$，$W_t$ 为独立于 $(z_0, r_1)$ 的 $\mathbb{R}^d$ 值 Wiener 过程，$\alpha, \beta, \sigma \in C^1([0,1]; \mathbb{R}_{\geq 0})$ 满足边界条件：

$$\alpha(0)=1,\quad \alpha(1)=0,\quad \beta(0)=0,\quad \beta(1)=1,\quad \sigma(0)=\sigma(1)=0$$

因此 $I_0 = z_0$，$I_1 = r_1$，特别地，$I_t|z_0$ 是从 $\delta_{z_0}$ 到 $\rho_c(r_1|z_0)$ 的一座桥。可以证明，满足如下 SDE 的解：

$$dX_t^{\text{SI}} = b(X_t^{\text{SI}}, z_0, t)\,dt + \sigma(t)\,dW_t, \qquad X_0^{\text{SI}} = z_0 \tag{1}$$

对所有 $t \in [0,1]$ 满足 $\text{Law}(X_t^{\text{SI}}) = \text{Law}(I_t|z_0)$，其中 $b: \mathbb{R}^d \times \mathbb{R}^d \times [0,1] \to \mathbb{R}^d$ 是如下损失的极小值点：

$$\mathcal{L}_{\text{SI}} = \int_0^1 \mathbb{E}\Big[\big|b(I_t, z_0, t) - \dot{\alpha}(t)z_0 - \dot{\beta}(t)r_1 - \dot{\sigma}(t)W_t\big|^2\Big]\,dt \tag{2}$$

期望取在联合分布 $(z_0, r_1, W_t)$ 上。特别地，$X_1^{\text{SI}} \sim \rho_c(r_1|z_0)$，因此求解 (1) 即可对 $\rho_c(r_1|z_0)$ 采样。

通过用数据样本离散化 (2) 中的期望，可以找到 $b$ 的近似极小值点。具体地，定义 $\hat{b}$ 为（在 2.3 节所定义的参数化族上）如下损失的极小值点：

$$\hat{\mathcal{L}}_{\text{SI}} = \frac{1}{N}\sum_{j=0}^{N-1}\mathbb{E}_{t\sim U(0,1)}\,\mathbb{E}_{\xi\sim\mathcal{N}(0,I)}\Big[\big|\hat{b}(I_{t,j}^\dagger, z_j^\dagger, t) - \dot{\alpha}(t)z_j^\dagger - \dot{\beta}(t)r_{j+1}^\dagger - \dot{\sigma}(t)\sqrt{t}\,\xi\big|^2\Big]$$

其中 $I_{t,j}^\dagger = \alpha(t)z_j^\dagger + \beta(t)r_{j+1}^\dagger + \sigma(t)\sqrt{t}\,\xi$。实践中，类似于 Karras 等人（2022），我们对损失进行重参数化，使 $\hat{b}$ 的输入和输出在所有时步 $t \in [0,1]$ 上近似具有零均值和单位方差，并对 $\alpha, \beta, \sigma$ 采用线性调度。

将此极小值点代入 (1)，得到 $\rho_c(r_1|z_0)$ 的近似模型：

$$d\hat{X}_t^{\text{SI}} = \hat{b}(\hat{X}_t^{\text{SI}}, z_0, t)\,dt + \sigma(t)\,dW_t, \qquad \hat{X}_0^{\text{SI}} = z_0 \tag{3}$$

我们期望 $\rho(\hat{X}_1^{\text{SI}}) \approx \rho_c(r_1|z_0)$，因为 $\hat{b} \approx b$。数值上，我们用一阶随机 Runge-Kutta 格式离散化 (3)。

#### 2.4.2 扩散模型（Diffusion Models, EDM）

我们的第二种建模方法基于扩散模型框架，特别是 Karras 等人（2022）的形式化。考虑如下 SDE 组：

$$dX_t^{F,0} = 0, \qquad X_0^{F,0} = z_0$$
$$dX_t^{F,1} = \sqrt{2\sigma(t)\dot{\sigma}(t)}\,dW_t, \qquad X_0^{F,1} = r_1$$

其中 $t \in [0, \infty)$，$(z_0, r_1) \sim \rho(z_0, r_1)$，$\sigma \in C^1([0,\infty); \mathbb{R}_{\geq 0})$ 单调递增且 $\sigma(0)=0$，$W_t$ 为独立于 $(z_0, r_1)$ 的 $\mathbb{R}^d$ 值 Wiener 过程。设 $p: \mathbb{R}^d \times \mathbb{R}^d \times [0,\infty) \to \mathbb{R}$ 为 $(X_t^{F,1}, X_t^{F,0})$ 的密度。现考虑反向时间 SDE：

$$dX_t^{\text{DM}} = -2\sigma(t)\dot{\sigma}(t)\nabla\log p(X_t^{\text{DM}}, z_0, t)\,dt + \sqrt{2\sigma(t)\dot{\sigma}(t)}\,d\overline{W}_t, \qquad X_T^{\text{DM}} = X_T^{F,1} \tag{4}$$

其中 $T > 0$，$\nabla$ 为对第一个分量的梯度，$\overline{W}_t$ 为独立于 $(z_0, r_1, W_t)$ 的反向时间 $\mathbb{R}^d$ 值 Wiener 过程。可以证明 $\text{Law}(X_t^{\text{DM}}) = \text{Law}(X_t^{F,1})$ 对 $t \in [0,T]$ 成立，特别地，$X_0^{\text{DM}} \sim \rho_c(r_1|z_0)$，因此求解 (4) 即可对 $\rho_c(r_1|z_0)$ 采样。

为此，我们从数据中估计 $\nabla \log p$。可以证明，$\nabla \log p$ 是如下损失的极小值点：

$$\mathcal{L}_{\text{DM}} = \int_0^T\mathbb{E}\Big[\big|\nabla\log p(X_t^{F,1}, X_t^{F,0}, t) - \sigma(t)^{-2}(X_t^{F,1} - r_1)\big|^2\Big]\,dt \tag{5}$$

因此，我们定义 $\hat{s}$ 为如下损失的极小值点：

$$\hat{\mathcal{L}}_{\text{DM}} = \frac{1}{N}\sum_{j=0}^{N-1}\mathbb{E}_{t\sim U(0,T)}\,\mathbb{E}_{\xi\sim\mathcal{N}(0,I)}\Big|\hat{s}(r_{j+1}^\dagger + \sigma(t)\xi,\,z_j^\dagger,\,t) - \sigma(t)^{-1}\xi\Big|^2$$

将此极小值点代入 (4) 并近似终端条件，得到 $\rho_c(r_1|z_0)$ 的近似模型：

$$d\hat{X}_t^{\text{DM}} = -2\sigma(t)\dot{\sigma}(t)\,\hat{s}(\hat{X}_t^{\text{DM}},z_0,t)\,dt + \sqrt{2\dot{\sigma}(t)\sigma(t)}\,d\overline{W}_t, \qquad \hat{X}_T^{\text{DM}} = \xi_T \tag{6}$$

其中 $\xi_T \sim \mathcal{N}(0,\,(1+\sigma(T)^2)I)$。我们期望 $\rho(\hat{X}_0^{\text{DM}}) \approx \rho_c(r_1|z_0)$，因为 $\hat{s} \approx \nabla\log p$ 且当 $T$ 足够大时 $\rho(\xi_T) \approx \rho(X_T^{F,1})$。数值上，我们用 Karras 等人（2022）引入的预测-校正方法离散化 (6)。

#### 2.4.3 基于 CRPS 的模型

我们的第三种建模方法学习一个直接生成映射 $\hat{f}: \mathbb{R}^d \times \mathbb{R}^p \to \mathbb{R}^d$，使得对于潜态 $z_0$ 和噪声 $\xi \sim \mathcal{N}(0, I_p)$，输出 $\hat{f}(z_0, \xi)$ 近似来自条件分布 $\rho_c(r_1|z_0)$ 的样本。训练通过最小化广义连续概率评分（CRPS）来完成，使用 $\ell_1$ 范数，这出于对天气预报鲁棒性的考虑，尽管它对一般分布并非严格正确的评分规则。

对于给定的 $(z_0, r_1)$ 对和独立噪声样本 $\xi, \xi' \sim \mathcal{N}(0, I_p)$，CRPS 目标为：

$$\mathcal{L}_{\text{CRPS}}(f) = \mathbb{E}\Big[\big|f(z_0,\xi) - r_1\big| - \frac{1}{2}\big|f(z_0,\xi) - f(z_0,\xi')\big|\Big] \tag{7}$$

其中第一项度量到目标的距离，第二项惩罚过大的集合离散度。

该 CRPS 目标仅通过边际分布比较预测集合与观测，并不显式施加联合空间结构。虽然标量 CRPS 是严格正确的，但这一性质不能推广到跨空间或多变量维度的聚合 CRPS，允许集合在点上精确但联合上不一致。这体现在两个方面：基于 CRPS 的模型不能保证物理上有意义的空间相关性，且实验中表现出谱偏差——捕捉大尺度模式的同时欠表示高频能量。

为解决这一问题，我们引入**谱正则化项**。设 $\mathcal{S}$ 为球谐变换（用 `torch-harmonics` 实现）。对球面 $S^2$ 上的空间场 $x(\theta, \phi)$，$\mathcal{S}$ 通过投影计算谱系数：

$$[\mathcal{S}(x)]_{l,m} = \int_{S^2} x(\theta,\phi)\,Y_{l,m}^*(\theta,\phi)\,d\Omega$$

其中 $Y_{l,m}$ 为正交归一球谐基函数，$d\Omega = \sin\theta\,d\theta\,d\phi$ 为面积元素，$(\cdot)^*$ 表示复共轭。我们在谱系数幅值 $|\mathcal{S}(\cdot)|$ 上计算 CRPS，确保相位不变性。

**与相关工作的比较**：Lang 等人（2024）观察到，公平 CRPS（无偏有限集合估计量）在大多数成员与观测精确匹配时可能出现退化，为缓解此问题，他们提出使用有偏和公平 CRPS 估计量的混合。类似地，Alet 等人（2025）限制潜噪声维度以约束随机方差并促进连贯的全局结构。相比之下，我们发现谱 CRPS 正则化不仅用标准双样本 CRPS 估计量稳定了高维噪声的训练，还允许全表达性随机条件化，无需有偏-公平混合或低维噪声约束。

在优化过程中，我们通过采样训练点 $z_j^\dagger$ 离散化外层期望，并用两个独立噪声样本近似内层期望：

$$\hat{\mathcal{L}}(f) = \Big[\big|f(z_j^\dagger,\xi_1) - r_{j+1}^\dagger\big| + \big|f(z_j^\dagger,\xi_2) - r_{j+1}^\dagger\big| - \big|f(z_j^\dagger,\xi_1) - f(z_j^\dagger,\xi_2)\big|\Big]$$

$$+ \lambda_{\text{spec}}\Big[\big||\mathcal{S}(f(z_j^\dagger,\xi_1))| - |\mathcal{S}(r_{j+1}^\dagger)|\big| + \big||\mathcal{S}(f(z_j^\dagger,\xi_2))| - |\mathcal{S}(r_{j+1}^\dagger)|\big| - \big||\mathcal{S}(f(z_j^\dagger,\xi_1))| - |\mathcal{S}(f(z_j^\dagger,\xi_2))|\big|\Big]$$

其中 $\xi_1, \xi_2 \sim \mathcal{N}(0, I_p)$ 独立同分布，$\lambda_{\text{spec}}$ 为权重因子。给定极小值点 $\hat{f}$，我们期望对于潜态 $z_0$ 和 $\xi \sim \mathcal{N}(0, I_p)$，$\hat{f}(z_0, \xi) \sim \rho_c(r_1|z_0)$，即单次 $\xi$ 采样通过 $\hat{f}$ 即得到条件分布的近似样本。

综合 2.2 节描述的潜空间编码策略、2.3 节描述的基于 Transformer 的解码器和预测架构，以及 2.4 节描述的概率模型，共同构成我们提出的 ATLAS 模型。

---

## 3. 实验设置

### 3.1 训练与评估

#### 数据集

我们在 $721\times1440$ 原始分辨率、6 小时间隔下使用 ECMWF ERA5 数据集的子集，1980–2019 年用于训练，2020 年用于测试。我们包含 7 个地面变量和 5 个大气变量（每个大气变量在 13 个不同气压层上），以及两个额外变量：海表温度（`sst`）和总降水（`tp`），共 **75 个输入和输出变量**。在任何陆地位置，`sst` 场赋值为零。

| 变量代码 | 描述 | ECMWF ID |
|---------|------|----------|
| `10u` / `10v` | 10 m 经/纬向风 | 165 / 166 |
| `100u` / `100v` | 100 m 经/纬向风 | 228246 / 228247 |
| `t2m` | 2 m 温度 | 167 |
| `msl` | 海平面气压 | 151 |
| `tcwv` | 整层垂直积分水汽 | 137 |
| `z---` | 位势（13 个气压层）| 129 |
| `t---` | 温度（13 个气压层）| 130 |
| `u---` / `v---` | 经/纬向风（13 个气压层）| 131 / 132 |
| `q---` | 比湿（13 个气压层）| 133 |

#### 训练过程

训练前，所有数据用训练年份计算的统计量归一化。具体地，对每个场分别计算所有像素值的均值和标准差，输入场和残差场分别计算独立的统计量，然后减去均值并除以标准差，使每个场近似服从标准高斯分布。评估时，对预测结果应用归一化的逆变换，并在未归一化的数据集上计算所有指标。

每个模型通过最小化其对应的损失函数（2.4 节详述）来训练，使用弹性平均随机梯度下降，总批大小为 32，分布在 32 张 80 GB A100 或 H100 GPU 上。我们使用来自 `optimi` 库、具有默认设置的 StableAdamW 优化器。学习率 $1.28\times10^{-4}$，前 2,000 步线性预热，随后 100,000 步余弦衰减——称为一个**周期**。之后学习率重置为原值的 $0.8\times$，再次余弦衰减 100,000 步。ATLAS-SI 和 ATLAS-EDM 训练 **3 个周期**，ATLAS-CRPS 训练 **6 个周期**。

所有预测模型使用同一个解码器，该解码器同样用上述设置训练 3 个周期，最小化其输出与残差场 $x_1 - x_0$ 之间的 $\ell_1$ 范数。为解决数据中潜在的非平稳性，解码器还以余弦天顶角（与日期相关）、地面位势、陆海掩膜和 `sst` 掩膜作为额外输入，均归一化到 $[-1,1]$ 范围，附加到 $x_0$ 上，共 **79 个输入变量**。

#### 评估过程

评估聚焦于在验证集（2020 年）上全球聚合的标准概率评分指标，并与 ECMWF IFS ENS（0.25 度水平分辨率的确定性物理集合基线）和 GenCast（强数据驱动基线）进行比较。

我们的评估协议使用跨 2020 年 28 个公共初始化日期的统计量：具体为每月 2 日和 16 日 UTC 00:00，加上 1–4 月的 8 日，共 28 个日期。我们的模型在 6 小时间隔上评估，展开至 15 天。每次评估同时模拟 **56 个集合成员**，在每个预报步计算集合均值 RMSE、CRPS 和离散-技巧比（SSR）。对于 GenCast 基线，我们用公开检查点（四分之一度，非业务）以与 ATLAS 相同的流程（但 12 小时步长）展开模型，以确保公平一致的比较。

#### 推理速度（单张 A100 GPU）

| 模型 | 单步耗时 | 步长 |
|------|---------|------|
| GenCast | ~140 秒 | 12 小时 |
| ATLAS-SI | ~94 秒 | 6 小时 |
| ATLAS-EDM | ~88 秒 | 6 小时 |
| ATLAS-CRPS | ~3.3 秒 | 6 小时 |

---

## 4. 结果

（结果图表请参阅原文。以下为关键定性结论。）

### 4.1 与 IFS 对比

ATLAS 三个变体相对 IFS-ENS 均取得显著技巧提升。地面风速（u10m、v10m）改进最强，CRPS 改进峰值接近 50%，前 6 天保持 10% 以上优势。大多数状态变量（q850、t850、地面风）全程 15 天保持优势。z500 是例外，约 12 天后技巧优势收敛至基线水平。

### 4.2 与 GenCast 对比

ATLAS-SI 在约 7 天内对 CRPS 和集合均值 RMSE 均一致优于 GenCast。7 天后统计上无显著差异。ATLAS-EDM 除 z500 略差外与 GenCast 基本相当。ATLAS-CRPS 在 8–10 天范围内有可检测的技巧提升，但前两天表现略弱。

关于集合离散度：ATLAS-SI 略微欠离散；GenCast 在长时间尺度上往往过度离散。ATLAS-CRPS 的 SSR 持续欠离散——CRPS 优化动力学似乎由最小化一阶误差主导，驱动集合离散的排斥力相对较弱，模型收敛到一个优先最小化预报误差而非最大化集合离散度的状态。

### 4.3 案例研究：丹尼斯风暴

对 ATLAS-SI 在 2020 年 2 月历史最强温带气旋之一——丹尼斯风暴上的表现进行了检验（预报从 2 月 11 日 UTC 00:00 初始化）。两个集合成员均准确捕捉了 2–4 天内的快速增强过程和完整风场演变。5 天预报同样较强，仅威尔士及大西洋向西区域风速略有低估。850 hPa 水平动能归一化谱分析：各预报时次两个成员的谱与 ERA5 高度吻合，高波数（>200）处存在轻微系统性欠表示，但整个预报期间无随时次变化的谱偏差趋势，表现出令人鼓舞的自相似动力学特征。

### 4.4 热带气旋评估

使用 2020 年 IBTRACs 数据集，对满足 7 天寿命条件的 46 个热带气旋进行评估，使用 TempestExtremes 跟踪算法，32 个集合成员。ATLAS-SI 集合均值路径误差在 80 小时后优于 GenCast，80 小时内基本相当；平均风速 MAE 在 144 小时内与 GenCast 相当，之后略有差距。典型案例（飓风 Eta、气旋 Damien、热带风暴克罗凡）中 ATLAS-SI 表现优于或持平 GenCast。

---

## 5. 潜空间建模的验证

尽管我们主张简化并使用标准架构，但在编码器和解码器的设计上，我们发现更定制化的方法是有益的。本节提供支持我们选择的实验证据。

### 基线自编码模型

学习自编码器经过调优，在所有 $16\times$ 空间压缩潜空间模型中达到最低的重建误差。模型结构如下：对均值-方差归一化输入施加正弦位置编码后，卷积层将输入嵌入到空间分辨率 $(195\times390)$，同时将隐藏维度增至 $832$。然后由 8 个 DiT 块处理此嵌入状态，每个 DiT 块的线性层逐步将隐藏维度减少 112 个通道。第 8 个块后，潜表示维度为 $64\times195\times390$。解码器与编码器对称，同为 8 个 DiT 块加最终线性层，逐步增大隐藏维度，最终投影回原始维度。

$64\times195\times390$ 的状态即为用于预测建模的潜空间。我们训练随机插值模型，评估两种不同预测方式：一是给定当前潜态预测下一潜态，二是给定当前潜态预测下一潜**残差**。另训练一个以状态残差为输入输出的第二个自编码器用于建模潜残差。

**关键实验结论**：即使 VAE 在 t850 和 q850 上重建误差较低（z500 误差相对较高），且 6 和 12 小时预报精度具有竞争力，长期展开性能仍落后于 IFS-ENS。此外，出人意料的是，尽管残差到残差的重建误差低于状态到状态，自回归展开时误差累积反而比潜态预测模型更快。

### 简化编码解码过程

我们发现，简单的双线性插值"编码"以牺牲小尺度信息为代价保留了潜空间结构。然而，用双线性上采样简单"解码"压缩后的状态会产生极高的重建误差。相对地，我们训练一个 DiT 解码器，以低分辨率残差和高分辨率输入状态为输入，预测高分辨率残差。可将该解码器理解为：预测与低分辨率残差和高分辨率输入均一致的高分辨率残差。这种方法不仅大幅降低了重建误差（在所有测试方法中达到最低），还产生了所有测试方法中最低的**展开误差**。

需要注意的是，尽管 DiT 骨干网络始终仅在低分辨率状态上运行，解码器在每个时步保留前一时步的高分辨率信息，从而在推理过程中维持高分辨率的时间一致性。

---

## 6. 结论

本文挑战了"概率天气预报需要复杂的领域特定架构或训练方法"的主流观点。我们提出了 ATLAS，一个简单可扩展的框架——由潜空间 Transformer 和局部投影器组成。结合任何概率估计框架（从随机插值到扩散模型和 CRPS 集合训练），均超越了业务 IFS 和深度学习模型 GenCast。

我们推测这些成功有两个关键因素：**扩展性**和**显式多尺度信号处理框架**。在初期开发中，我们发现持续增加骨干网络的计算复杂度能稳定提升预报技巧，这似乎促进了模型自然学习物理相关性的能力。从这一角度来看，Transformer 架构固有的可扩展性及其在非天气领域丰富的扩展工具，可能预示着未来向 ATLAS 类架构收敛的趋势。此外，我们的方法将学习全分辨率状态的时间演化与预测更大水平尺度的基本大气动力学两个任务解耦，允许分别扩展各任务的模型。

我们进行了广泛的模型评估，证明了在聚合指标上具有统计显著性的改进、对极端事件的清晰模拟，以及在计算成本低于 GenCast 的条件下对热带气旋的精确跟踪。这些结果表明，通往更高保真度的路径不在于架构复杂性，而在于通用基础模型的扩展。

**局限性**：我们的基线虽然是有意选择的（兼具已知优势和在完全开放 ERA5 训练数据约束下可行的比较），但并非唯一值得衡量技巧的强基线。在 ERA5 数据之外对 ATLAS 进行微调（例如在业务 IFS 分析上）将有助于与 AIFS、FGN 等更当代基线进行比较。同样，对热带和中纬度气旋以外现象的额外验证，以及超出 2020 年的分析，对于更全面理解 ATLAS 的行为是必要的。

关于扩散模型，宽噪声调度可捕捉尾部行为并实现优秀的短期预报（如一周），但往往损害较长时间尺度的技巧。因此，我们刻意牺牲了部分近期技巧以换取稳定性，相信改进噪声调度设计和更严格的过拟合控制可缓解此问题。

最后，由于扩散模型具有良好的可控性，基于扩散的天气预报成功可被视为为**引导式预报**开辟道路——例如，将集合引导到验证为特定区域危害的子集合，正如 Brenowitz 等人（2025）所证明的那样。因此，这一技术进步有望推动整个社区将 AI 天气模型从狭义的、任务特定的技术，演进为更广泛、多任务多元化的基础信息学系统。

---

*TeX 源码位置：`2601.18111/main.tex`*
